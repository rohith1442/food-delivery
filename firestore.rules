rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // All Firestore access must go through our NestJS backend.
    // Firebase Admin SDK bypasses Firestore Security Rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
