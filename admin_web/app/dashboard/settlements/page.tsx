"use client";
import { useEffect, useState } from "react";
import { api } from "@/lib/api";
type Settlement = { id: string; orderId: string; partyType: string; netAmount: number; status: string; provider: string; providerTransferId?: string; providerPayoutId?: string; createdAt: string };
export default function SettlementsPage() { const [items, setItems] = useState<Settlement[]>([]); useEffect(() => { void api.get<{ settlements: Settlement[] }>("/admin/settlements").then((response) => setItems(response.data.settlements ?? [])); }, []); return <main className="p-8"><h1 className="text-3xl font-bold">Settlements</h1><div className="mt-8 overflow-x-auto rounded-xl bg-white shadow"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-4">Order</th><th className="p-4">Party</th><th className="p-4">Net</th><th className="p-4">Status</th><th className="p-4">Provider</th><th className="p-4">Provider ID</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b"><td className="p-4">{item.orderId}</td><td className="p-4">{item.partyType}</td><td className="p-4">₹{item.netAmount}</td><td className="p-4">{item.status}</td><td className="p-4">{item.provider}</td><td className="p-4">{item.providerTransferId ?? item.providerPayoutId ?? "-"}</td></tr>)}</tbody></table></div></main>; }
